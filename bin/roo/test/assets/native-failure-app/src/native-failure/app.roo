(ns native-failure.app
  (:require roo.cli.test.native))

(defun main
  "Raise a native-backed error for launcher teardown testing.

  Args:
  - `args`: Command line arguments, which are unused by this fixture.

  Returns:
  Never returns; raises the native test error."
  [args]
  (roo.cli.test.native/fail nil))
