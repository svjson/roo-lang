(ns lookup.doc-test
  (:require proof.core
            [lookup.doc :as doc]))



;; lookup.doc/strip-backticks

(deftest strip-backticks/removes-one-surrounding-pair
  (is (= "name" (doc/strip-backticks " `name` ")))
  (is (= "name" (doc/strip-backticks "name"))))
