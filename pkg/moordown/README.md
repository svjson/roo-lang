# moordown

Small Markdown construction and rendering functionality for Roo packages.

`moordown` builds Markdown from simple data structures instead of scattered
string concatenation. The initial API covers headings, paragraphs, fenced code
blocks, links, tables, and complete documents.
