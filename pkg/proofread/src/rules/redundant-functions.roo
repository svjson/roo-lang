(ns proofread.rules.redundant-functions
  (:require [proofread.rules.redundant-functions.redundant-function :as redundant-function]
            [proofread.rules.registry :as registry]))

(def rules
  "Redundant-function rule descriptors."
  (registry/descriptors
    :redundant-functions
    [[:redundant-function
      redundant-function/check
      {:default-options {:ignore-function-names ["check" "check-file"]}
       :check-with-options redundant-function/check-with-options}]]))

(defun check
  "Find redundant function diagnostics for a parsed AST root.

  Args:
  - `root`: A top-level `roo.ast` node map.

  Returns:
  A vector of diagnostic maps."
  [root]
  (registry/check rules root))
