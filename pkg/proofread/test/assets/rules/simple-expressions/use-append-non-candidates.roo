(ns proofread.simple-expressions-use-append-non-candidates)

(def keep-using-append
  (concat (:errors options)
          ["Missing output directory"
           "Missing input file"]))

(def multi-concat
  (concat (:errors options)
          (concat ["Missing output directory"])))
