# libroo

`libroo` is the core embeddable Roo runtime.

It contains the language implementation, evaluator, namespace system, standard
library namespaces, runtime value model, filesystem abstraction, and C++ host
interop APIs. 

Most embedding examples live in the repository root [README](../../README.md).
