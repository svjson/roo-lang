# lisple-server

`lisple-server` is a small server layer for Lisple message parsing, dispatch,
and server experiments.

It is a sibling library to `liblisple`, not part of the core runtime. The code
is currently primitive and should be treated as an integration experiment rather
than the model for new Lisple application architecture.
